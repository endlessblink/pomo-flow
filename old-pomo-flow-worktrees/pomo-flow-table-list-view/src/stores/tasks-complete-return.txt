// State
    tasks,
    projects,
    currentView,
    selectedTaskIds,
    activeProjectId,
    activeSmartView,
    activeStatusFilter,
    hideDoneTasks,

    // Computed
    filteredTasks,
    tasksWithCanvasPosition,
    tasksByStatus,
    totalTasks,
    completedTasks,
    totalPomodoros,

    // Helper functions
    getTaskInstances,

    // Undo/Redo enabled actions
    ...undoRedoEnabledActions()
  }

  return {
    // State
    tasks,
    projects,
    currentView,
    selectedTaskIds,
    activeProjectId,
    activeSmartView,
    activeStatusFilter,
    hideDoneTasks,

    // Computed
    filteredTasks,
    tasksWithCanvasPosition,
    tasksByStatus,
    totalTasks,
    completedTasks,
    totalPomodoros,

    // Helper functions
    getTaskInstances,

    // Undo/Redo enabled actions
    ...undoRedoEnabledActions()
  }
})